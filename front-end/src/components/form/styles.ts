import styled, { AnyStyledComponent } from 'styled-components';

export const Form: AnyStyledComponent = styled.form`
  display: flex;
  flex-direction: column;
  margin: 0 auto;
  width: 100%;
  max-width: 600px;
  justify-content: center;
`